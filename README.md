# reparam_autoencoder

This project implement the idea in paper "Auto-Encoding Variational Bayes"

So given any set of Bernoulli distributed data, this model will learn the underlyding latent structure of data

We use mnist binary image data set as a tester.
